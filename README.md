# README.md

## TaskFlow — Корпоративный задачник

**TaskFlow** — это учебное веб-приложение, разработанное для автоматизации учёта и контроля выполнения задач в компании. Проект создан на основе существующего веб-чата WebChat и адаптирован под нужды управления проектами.

Проект сдан в рамках учебного задания для компании ООО «Айти Сервис».

---

###  Описание проекта

В компании отсутствовал единый инструмент для управления задачами, что приводило к потере информации и срывам сроков. TaskFlow решает эту проблему, предоставляя простой интерфейс для создания, просмотра и отслеживания задач в рамках проектов.

**Технологии:**
*   **Backend:** ASP.NET Core 8.0 (Razor Pages), Entity Framework Core.
*   **База данных:** SQL Server LocalDB.
*   **Frontend:** HTML5, CSS3, Bootstrap 5.

---

###  Критерии приёмки

Проект считается успешно сданным, если выполняются следующие условия:
1.  Приложение запускается командой `dotnet run` без ошибок.
2.  При первом запуске автоматически создаётся база данных.
3.  Открывается страница `/Tasks`, отображающая таблицу задач.
4.  Работает форма создания новой задачи по адресу `/Tasks/Create`. После отправки задача появляется в общем списке.
5.  Для каждой задачи в списке есть кнопка «Выполнено», которая меняет её статус на `Done`.
6.  Данные (задачи и проекты) сохраняются в базе и не теряются после перезапуска сервера.
7.  Интерфейс аккуратный, использует стили Bootstrap, нет сломанных элементов.

---

###  Инструкция по запуску

Для запуска проекта на вашей машине выполните следующие шаги:

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/<ваш_логин>/TaskFlow.git
    cd TaskFlow
    ```

2.  **Восстановите зависимости:**
    В терминале выполните команду:
    ```bash
    dotnet restore
    ```

3.  **Примените миграции базы данных:**
    Это создаст необходимые таблицы (Users, Projects, Tasks, UserProjects) в SQL Server LocalDB.
    ```bash
    dotnet ef database update
    ```
    *Примечание: Если вы используете Visual Studio, можно также запустить проект, и миграции применятся автоматически.*

4.  **Запустите приложение:**
    ```bash
    dotnet run
    ```

5.  **Откройте браузер** и перейдите по адресу: `https://localhost:7005` (или другому порту, указанному в консоли).

---

###  Структура проекта

** Уже существующий проект WebChat надо переделать под TaskFlow **


* Шаг 1. Скопировать папку WebChat, переименовать копию в TaskFlow, открыть проект.

* Шаг 2. Переименовать пространства имён: все вхождения WebChat заменить на TaskFlow.

* Шаг 3. Переименовать модели: Message.cs → Task.cs (добавить поля Status, Priority, DueDate, AssigneeId), Room.cs → Project.cs (добавить поле Description), UserRoom.cs → UserProject.cs (без изменений по сути, только переименовать класс). Класс User оставить без изменений.

* Шаг 4. Обновить AppDbContext: заменить DbSet<Message> на DbSet<Task>, DbSet<Room> на DbSet<Project>, DbSet<UserRoom> на DbSet<UserProject>. Настроить связи по аналогии с тем, как были настроены связи в WebChat (один ко многим для Task→Project, Task→User, многие ко многим через UserProject). Добавить начальные данные — хотя бы один проект и одну задачу для демонстрации.

* Шаг 5. Создать TaskService вместо ChatService. Методы: CreateTaskAsync — создание задачи, GetProjectTasksAsync — получить задачи проекта, GetMyTasksAsync — получить задачи текущего пользователя, UpdateTaskStatusAsync — обновить статус задачи. Логика методов повторяет логику методов ChatService, только вместо сообщений — задачи.

* Шаг 6. Создать страницы: Pages/Tasks/Index.cshtml — таблица со списком задач, кнопки для изменения статуса, кнопка «Создать задачу». Pages/Tasks/Create.cshtml — форма для создания задачи. Pages/Projects/Index.cshtml — список проектов.

* Шаг 7. Зарегистрировать TaskService в Program.cs как scoped сервис. Настроить маршруты.

* Шаг 8. Запустить проект, выполнить миграции, проверить работу.

---

###  Скриншоты

Ниже будут представлены скриншоты ключевых страниц приложения после выполнения:

1.  **Главная страница со списком задач (`/Tasks`):**
    
2.  **Форма создания новой задачи (`/Tasks/Create`):**
    
3.  **Демонстрация изменения статуса на "Выполнено":**
    

---

###  Автор

*   Студент: Ярослав
*   Контактное лицо от заказчика: Алексей, руководитель отдела разработки ООО «Айти Сервис».
